%h1 Hashtags#index

- @hashtags.each do |hashtag|
  - unless hashtag.bookmarks.empty?
    %div.row
      %h3= link_to hashtag.tag, hashtag_path(hashtag) 
      = render partial: "bookmarks/bookmark", collection: hashtag.bookmarks.limit(5)
